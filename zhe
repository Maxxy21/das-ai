export default function ChatInput({ /*...props*/ }) {
  // ... your existing hooks and state

  return (
    <div className="fixed inset-x-0 bottom-0 p-4">
      {/* Chat header and other content go here */}

      <div className="flex flex-col h-full justify-between">
        <div className="overflow-y-auto space-y-4 p-4">
          {/* Map over your messages here */}
          {messages?.map((message) => (
            <article
              key={message._id}
              className={`p-4 ${message.author === NAME ? "bg-blue-600 text-white" : "bg-gray-100"} rounded-lg shadow transform transition-opacity duration-150 ease-in-out`}
            >
              <div className="font-medium text-lg">{message.author}</div>
              <p className="text-gray-700 mt-1">
                {message.body}
              </p>
            </article>
          ))}
        </div>

        {/* Chat input */}
        <form
          onSubmit={/*...*/}
          className="mt-4 flex shadow-lg bg-white/80 backdrop-blur-sm rounded-full border border-transparent focus-within:border-blue-500"
        >
          <input
            type="text"
            value={newMessageText}
            onChange={/*...*/}
            placeholder="Say something..."
            className="flex-grow p-4 bg-transparent text-lg rounded-l-full placeholder-gray-500 focus:outline-none"
          />
          <button
            type="submit"
            disabled={!newMessageText}
            className="w-12 h-12 bg-blue-500 rounded-full text-white flex items-center justify-center"
          >
            {/* Icon or text for send button */}
          </button>
        </form>
      </div>
    </div>
  );
}
